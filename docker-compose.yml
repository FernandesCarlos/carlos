services:
  alunos-app:
    build: .
    container_name: alunos-container
    ports:
      - "8080:80"  # Porta do host -> porta do container
    volumes:
      - .:/usr/share/nginx/html:cached  # live reload: atualiza arquivos automaticamente
    restart: unless-stopped
